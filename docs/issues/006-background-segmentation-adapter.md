# [HITL] 接入 ComfyUI 现有人像分割模型

## 父问题

- 依赖：005-render-overlay-node

## 要构建什么

实现可插拔的人像分割适配器，优先复用 ComfyUI 已安装的高质量分割/抠像模型，为透明背景和图片背景提供 mask。

## 验收标准

- 能发现并调用已配置的分割模型。
- 透明背景输出人物边缘质量可检查。
- 没有可用模型时显示明确提示，并可选择关闭透明背景或使用低质量回退。
- 分割失败不会破坏原视频背景模式。
